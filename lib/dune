(menhir
 (flags (--explain))
 (modules p))

(ocamllex
 (modules l))

(library
 (name qash)
 (preprocess
  (pps lwt_ppx ppx_deriving.make ppx_deriving.show ppx_yojson_conv))
 (libraries
  caqti-driver-sqlite3
  caqti-lwt
  cmdliner
  csv
  dream
  fsnotify
  jingoo
  logs.fmt
  pcre))
